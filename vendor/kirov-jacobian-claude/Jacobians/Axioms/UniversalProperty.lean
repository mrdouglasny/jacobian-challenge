import Jacobians.Axioms.TorusAlbanese

/-!
# Compatibility import for universal-property axioms

The universal-property textbook leaves now live in
`Jacobians/Axioms/TorusAlbanese.lean`, which owns the four vetted declarations:

* `AX_torus_oneforms_dualCover`
* `AX_torus_self_albanese`
* `AX_period_functoriality`
* `AX_curve_generates_jacobian`

This module remains as a compatibility import for older references to
`Jacobians.Axioms.UniversalProperty`.
-/
