# Code owners — owner-vetted files.
#
# These set the project's rules and trust boundary. GitHub requires a review from
# the listed owner before a PR touching them can merge (when "Require review from
# Code Owners" branch protection is on). Collaborators' agents may PROPOSE edits via
# PR, but they do not merge without @mrdouglasny's review. See CLAUDE.md / AGENTS.md.

# Agent + project rules
/CLAUDE.md                              @mrdouglasny
/AGENTS.md                              @mrdouglasny
/.github/CODEOWNERS                     @mrdouglasny

# CI / automation (the machine gates)
/.github/                               @mrdouglasny

# The soundness/consistency machinery — the trust root CI relies on.
# AXIOM_AUDIT.md and docs/axiom-report.txt are deliberately NOT listed:
# axiom-report.txt is regenerated and diffed against the kernel by CI, so it
# cannot diverge from the kernel without failing the build; AXIOM_AUDIT.md is
# documentation with no kernel/CI soundness power. Protecting the generator and
# checker below is what actually guards the axiom ledger.
/scripts/axiom_report.lean              @mrdouglasny
/scripts/check_axiom_consistency.sh     @mrdouglasny
