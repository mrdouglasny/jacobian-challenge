# Project status — 2026-06-06

A current, verified snapshot of where `jacobian-challenge` stands, written
to accompany the `ofcurve-inj` → `main` pull request. This **supersedes**
[`status-2026-05-31.md`](status-2026-05-31.md) (which predates the
2026-06-04/05 discharge push and still reports 93 axioms). All numbers below
were measured directly from the source tree and a clean `lake build` on
2026-06-06.

## TL;DR

- **`lake build` is green** — 8597 jobs, exit 0, verified 2026-06-06.
- **62 axioms total**, all in our own modules (the vendored Kirov + Wallace
  subtrees are now axiom-free). Down from 93 on 2026-05-31 — a 31-axiom net
  reduction this window. Canonical, kernel-verified, triaged in
  [`AXIOM_AUDIT.md`](../AXIOM_AUDIT.md). *(The final drop 63→62 was the E6
  discharge of `AX_torus_descent_holo` to a `theorem`, 2026-06-06.)*
- **Sorry accounting splits into two categories:**
  - **11 gap-layer `sorry`** — real holes, all outside the core
    (`Extensions/Hyperelliptic.lean` 6, `Extensions/AbelJacobi.lean` 4,
    `ProjectiveCurve/Hyperelliptic/AntiInvariance.lean` 1).
  - **8 anchor-layer `sorry`** — *intentionally* deferred proofs of
    Gemini-vetted RR/Serre statements (`RiemannRochAPI` 7,
    `SerreDualityAPI` 1); `PluckerAPI` is fully proved and two of the three
    Serre theorems (the dimension-form duality + the h⁰−h¹ identity) are now
    real; the value is the locked-down
    statement, the proof comes last (see "Anchor APIs" in the README).
- The Buzzard API closure, the core Jacobian / complex-torus layer, the
  concrete curve witnesses, and the hyperelliptic 1-form framework remain
  **sorry-free**.

## Repository / remote state

| | |
|---|---|
| Branch | `ofcurve-inj` |
| Ahead of `main` | 52 commits |
| Commits on branch | 392 |
| `solo` | frozen pre-import snapshot (v0.2, 2026-04-25) |
| Active work window | heavy discharge push 2026-06-04 → 2026-06-06 |

## Build

```
lake build   # 8597 jobs, exit 0, green on 2026-06-06
```

- Lean `v4.30.0`; Mathlib pinned by the challenge.
- Warnings present (linters + the `declaration uses sorry` notices). No errors.

## Headline results landed this window

The two strongest **anti-degeneracy** targets — the properties that would
expose a vacuous "Jacobian" — are now both achieved:

1. **General positive-genus Abel injectivity is a THEOREM, not an axiom.**
   `AX_ofCurve_inj` is retired; `Axioms/OfCurveInjective.lean` derives
   injectivity of the Abel–Jacobi map on any positive-genus compact Riemann
   surface from (a) `AX_Period_Triangle` (the period 1-cocycle axiom,
   deep-think-vetted) giving basepoint-independence, (b) `AX_AbelTheorem`
   (degree-0 restricted) placing `(Q₁)−(Q₂)` in `PrincipalDivisors`, and (c)
   `principal_imp_eq_of_genus_pos` — the **proven, axiom-free** genus
   obstruction (a degree-1 map to ℙ¹ forces genus 0). This was a one-for-one
   axiom swap (`−AX_ofCurve_inj +AX_Period_Triangle`).
   - Elliptic witness `elliptic_ofCurve_injective` proved separately, kernel-
     clean (standard-3 + cycle-basis axioms, not even `AX_PeriodLattice`).

2. **Albanese categoricity is PROVED.** `ofCurve_isJacobian`
   (`UniversalProperty.lean`): for genus > 0 the concrete `Jacobian X` /
   `ofCurve` *satisfy* the Albanese universal property — every pointed
   holomorphic map to a complex torus factors uniquely through `ofCurve` by a
   holomorphic group hom. Genuine `∃!`; rests on 4 vetted torus axioms +
   `AX_curve_generates_jacobian`; `#print axioms`-clean (no `sorryAx`). Pins
   the Jacobian up to unique isomorphism — stronger than Buzzard's API asks.

Plus the still-real earlier headlines:
- `genus ProjectiveLine = 0`, `genus (Elliptic ω₁ ω₂ h) = 1` — derived
  theorems, **no project axioms**.
- `genus (HyperellipticEvenProj H) = H.f.natDegree / 2 − 1` — derived theorem
  (S1–S7 framework + Liouville hierarchy), modulo Liouville L2/L3 only.

## De-opaquing and anchors this window

- **`loopIntegralToH1`** (the period / path-independence pairing — the deepest
  analytic gap) discharged to a real `def` via the multi-chart line integral
  over the analytic cycle basis (2026-06-05).
- **`pathIntegralBasepointFunctional`** → real `∫` def; **`PrincipalDivisors`**
  → `range divHom`; **`H0`** → the real `riemannRochSpace D` (retiring 3 opaque
  H0 axioms, 66→63).
- **`AX_torus_descent_holo` discharged** to a real `theorem` (2026-06-06, 63→62):
  the descended quotient map is `ContMDiff ω` via the local-section route over
  Kirov's `ZLatticeQuotient` local-homeomorphism API, composed with
  `P.fromQuot_holo`. Removes the last Albanese-side fallback axiom.
- **Anchor APIs** for the research-grade cluster (sheaf-coh / RR / Serre /
  Plücker): faithful, Gemini-vetted statements pinned first, proofs deferred —
  `riemannRochSpace`, `RiemannRochAPI`, `SerreDualityAPI`, `PluckerAPI`, gated
  by the `SheafCohomologySpec` faithfulness suite.

## Axiom landscape (62, by topic)

See the README "Axiom landscape" table and [`AXIOM_AUDIT.md`](../AXIOM_AUDIT.md)
for the authoritative per-axiom view. Coarse topic split:

| Topic | ~Count | Difficulty |
|-------|:------:|:----------:|
| Period / Hodge / homology core | ~8 | 🔴🟡 |
| Abel–Jacobi (`AX_AbelTheorem`; `ofCurve_inj` now a theorem) | 1 | 🔴 |
| Sheaf-coh / RR / Serre / Plücker (`H0` de-opaqued) | ~9 | 🔴 |
| Functoriality (pushforward / pullback) | ~9 | 🟡 |
| Torus / Albanese universal property | 5 | 🟡 |
| Concrete curves (atlases & witnesses) | ~26 | 🟢🟡 |
| Liouville hierarchy L2 / L3 | 2 | 🔴 |

**Hygiene caveat (unchanged):** every axiom was authored/curated in-session and
LLM-cross-vetted (Gemini deep-think + Codex); **none has had independent human-
mathematician review.** The axiom files are where an outside reviewer should
look first.

## Open workstreams (priority order)

1. **Hyperelliptic-odd atlas** (`#14`): mirror the sorry-free even-side
   framework to clear the 6 `Extensions/Hyperelliptic.lean` gap-sorries.
3. **`AX_Period_Triangle` discharge** via the developing-map construction
   (well-definedness + base case done; bridge + telescoping remain) — would
   retire the last analytic input to general Abel injectivity.
4. **Anchor proofs (last):** Čech/Dolbeault for RR/Serre, Bézout/adjunction
   for Plücker — against the now-locked statements.
5. **Fully axiom-clean even-genus:** the Liouville **L2 core** (canonical-
   differentials theorem), scoped in
   [`genus-L2-L3-discharge-plan.md`](genus-L2-L3-discharge-plan.md).

## How this snapshot was produced

```bash
git rev-parse --abbrev-ref HEAD                 # ofcurve-inj
git rev-list --count main..HEAD                 # 52
lake build                                       # 8597 jobs, exit 0
grep '^**Active project axioms' AXIOM_AUDIT.md   # 62 (kernel-verified)
```
