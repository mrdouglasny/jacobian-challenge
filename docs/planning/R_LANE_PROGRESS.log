# Lane R progress log — feat/keystone-r0-signtest
# Scope: R0 (sign test) + R1 (germ→chart-coefficient layer) + R2 (PoU split), per
# docs/planning/S3_FINESHEAF_RES_SCOPING.md.

[2026-06-10 ~start] Session start. Read S3 scoping in full; surveyed
DbarDisk.lean (cauchyPompeiu_area : ∫ dbar g / (ζ−z) = −π·g z, PROVEN),
DolbeaultComparisonInverse.lean (cechPoU/rhoC/dbarRho), port layout
(vendor/kirov-dolbeault-port, lean4 v4.30.0, mathlib c5ea0035, build cache hot).
R0 plan: dbar Leibniz (fderiv_mul) + dbar_eq_zero_of_differentiableAt away from
the pole ⇒ dbar(χ·(z−a)⁻¹) =ᵐ (dbar χ)/(ζ−a) ⇒ integral = −π·χ(a) by
cauchyPompeiu_area at z = a.  Expected convention: ∫_ℂ ∂̄(χ·(·−a)⁻¹) dA_Leb = −π,
so the normalization constant for the residue ladder is −π⁻¹ = (2πi)⁻¹·(−2i),
i.e. Forster's (2πi)⁻¹∬τ with τ read against dz∧dz̄ = −2i·dA.  Writing
KirovDolbeault/Dolbeault/FineResidue/SignTest.lean now.

[R0 DONE] SignTest.lean compiles clean (lake env lean), all 6 decls
#print-axioms-clean ([propext, Classical.choice, Quot.sound]).
PINNED CONVENTION: ∫_ℂ ∂̄(χ·(z−a)⁻¹) dA_Leb = −π·χ(a)  (integral_dbar_smearedSimplePole);
normalizer resNormalization := −π⁻¹ = (2πi)⁻¹·(−2i) gives
resNormalization · ∫ = 1 for χ(a)=1 (resNormalization_integral_eq_one).
No fudge: raw Forster (2πi)⁻¹ on the Lebesgue integral would give i/2; the −2i Jacobian
(dz∧dz̄ = −2i·dA) is recorded as resNormalization_eq_two_pi_I_inv_mul.
Non-vacuity witness via ContDiffBump (exists_signTest_witness). Committing R0; next R1.

[R1 DONE] OneOneCoeff.lean compiles clean; 5 headline decls axiom-clean.
The (1,1) chart-coefficient layer: chartMap/transitionMap (+ analyticAt,
deriv_ne_zero, relocation transitionMap_chartMap from the proven
transition_analyticAt_of_mem / transition_deriv_ne_zero atoms in
CechModelManifold.lean), OneOneLawAt (germ-eventual |φ'|² law via Complex.normSq),
oneOneCoeff : Submodule ℂ (ι → ℂ → ℂ) (free ℂ-linearity for R4), and
isOneOneCoeff_congr (germ-robustness, scoping risk item 2). Committing R1; next R2
(PoU split reusing cechPoU/rhoC/dbarRho from DolbeaultComparisonInverse).

[R2 DONE] PoUSplit.lean compiles clean (no warnings after omit [Nonempty X]);
3 headlines axiom-clean. pouSplit σ_i := ∑_k ρ_k·(w k i) over the REUSED
cechPoU/rhoC backbone; contMDiffAt_pouSplit (gdTerm support-aware gluing pattern);
pouSplit_telescope (support-aware refinement of cechCoboundary_telescoping —
cocycle identity demanded only on triple overlaps, IsOverlapCocycle);
isOverlapCocycle_sub gives the ML difference-family shape w i j = v j − v i.
M1 scope (R0+R1+R2) complete; committing R2.

[SESSION END] Commits on feat/keystone-r0-signtest: a1d7ac9 (R0), 72c06c6 (R1),
6066025 (R2). All FineResidue files sorry-free, lake-env-lean-validated,
headlines axiom-clean. Not pushed (orchestrator gates).
R3 next: Wirtinger chain rule dbar (f∘φ) = ((dbar f)∘φ)·conj φ′ for holomorphic φ
(planar, fderiv composition + DbarDisk.dbar), then: pouSplit read in chart-j
coordinates, dbar of it = member of oneOneCoeff (uses pouSplit_telescope +
dbar_eq_zero_of_differentiableAt on w, transitionMap_analyticAt/deriv_ne_zero
from OneOneCoeff.lean). Cite R0's resNormalization, do not re-derive signs.

# ---- Session 2: R3 (feat/keystone-r3-glue) ----

[R3a DONE] ChainRule.lean compiles clean (lake env lean); dbar_comp axiom-clean
([propext, Classical.choice, Quot.sound]).  The planar Wirtinger chain rule:
  dbar (f ∘ φ) z = dbar f (φ z) * (starRingEnd ℂ) (deriv φ z)
for φ holomorphic at z, f merely ℝ-differentiable at φ z.  Proof: fderiv_comp +
HasDerivAt.complexToReal_fderiv (the same atom dbar_eq_zero_of_differentiableAt uses),
then real-basis (1, I) decomposition of the ℝ-linear fderiv of f; the I² = -1 step via
linear_combination Complex.I_sq (plain `ring` can't use it).  Own file, not appended to
SignTest: SignTest is the frozen R0 gate ("cite, do not re-derive"); ChainRule imports it
so the planar calculus dbar_mul -> dbar_comp stays linearly ordered for R4-R6.

[R3 DESIGN NOTE - LAW-SHAPE ANALYSIS; READ BEFORE BUILDING R4] The handoff's literal
slot-free claim "(dbar (pouSplit ∘ (chart j).symm))_j ∈ oneOneCoeff" is NOT provable over
the landed R2 interface, and is false for nontrivial data: R2's w is SCALAR overlap data
(plain additive IsOverlapCocycle - the ML m_j - m_i shape; a form-coefficient family
cannot satisfy the plain additive triple-overlap identity across charts), so the
telescope forces s_k∘φ - s_j = (holomorphic), pinning the transformation of ∂̄s to the
(0,1) factor conj φ' ALONE.  A family obeying both the (0,1) and the (1,1) law would
need φ' ≡ 1 wherever it is nonzero.  R1's OneOneLawAt was NOT adjusted - it is correct
as landed (normSq φ' is what Lebesgue change-of-variables patches in R4).  The factors
reconcile exactly as the handoff's NOTE says: the second φ' enters via the holomorphic
change of the dz-slot, which on scalar overlap data must be an explicit input - the
holomorphic (1,0) chart-coefficient family IsOneZeroCoeff g (overlap law
g_j = (g_k ∘ φ)·φ', i.e. the chart coefficients of a global holomorphic 1-form; in the
residue application, of the S1 canonical form ω₀ with K = div ω₀).  Headline family:
t_j := dbar(s_j)·g_j  =  the chart coefficient of Forster's τ = ∂̄(σ_scalar·ω₀).
APPLICATION CONSTRAINT recorded for R4/R6: at K = div ω₀ ≠ 0 (genus ≥ 2) the scalar
Z¹(O_K) data has poles at K-points, so HolomorphicOnOverlaps requires the chart-disk
cover REFINED so each of the finitely many K-points lies in a single cover set (then
w k j is holomorphic on every overlap, and at a K-point of U_j only the k = j term of
σ_j survives, killed by the cocycle diagonal w j j = 0).  Genus 1: K = 0, no refinement.
The ω₀-witness for IsOneZeroCoeff is a later lane-R rung (germ-extraction layer).

[R3b DONE] Glue.lean compiles clean (lake env lean, no warnings); all 4 proof headlines
axiom-clean ([propext, Classical.choice, Quot.sound]): contDiffAt_splitCoeff,
splitCoeff_transition_eventuallyEq, dbar_splitCoeff_transition, glueCoeff_mem_oneOneCoeff.
splitCoeff 𝔇 w j := pouSplit 𝔇 w j ∘ (chartAt ℂ (𝔇.center j)).symm (the handoff's s_j);
contDiffAt_splitCoeff via contMDiffAt_pouSplit + contMDiffOn_chart_symm through the
RealManifold bridge; splitCoeff_transition_eventuallyEq = pouSplit_telescope read in
chart-j coordinates (germ-eventual, per the R1 germ discipline);
dbar_splitCoeff_transition = the (0,1) law (chain rule dbar_comp +
dbar_eq_zero_of_differentiableAt on ŵ + DbarOpenDisk.dbar_add);
glueCoeff_mem_oneOneCoeff = THE R3 HEADLINE: glueCoeff 𝔇 w g (= fun j z =>
DbarDisk.dbar (splitCoeff 𝔇 w j) z * g j z) ∈ oneOneCoeff 𝔇 for w smooth + cocycle +
holomorphic-on-overlaps and g an IsOneZeroCoeff slot family; normSq φ' reassembled by
Complex.mul_conj.  resNormalization cited (Glue/ChainRule docstrings), not re-derived.
Import gotcha (pre-existing in snapshot): GluedDbarDatum/CechFinitenessBallSolve clashes
with DolbeaultComparisonInverse (duplicate `rhoC`); used the snapshot's own local copy
ChartDiskCover.contDiffAt_dbar_chartDisk (ChartDiskFiniteness) instead - same collision
the snapshot already documents at that lemma.
R4 NEXT NEEDS: (i) the integral functional I(c) := Σ_j ∫ ρ̃_j · t_j over glueCoeff
members (ℂ-linearity free from oneOneCoeff being a Submodule); (ii) the chart-relocation
lemma via integral_image_eq_integral_abs_det_fderiv_smul + det_restrictScalars +
norm_complex_apply, whose Jacobian is literally OneOneLawAt's normSq factor;
(iii) the ω₀-coefficient witness for IsOneZeroCoeff (+ the K-point-separating cover
refinement above) before R6 can evaluate on real Z¹(O_K) data; (iv) cite
resNormalization = -π⁻¹ from SignTest for the final normalization - do not re-derive.

# ---- Session 3: R4 (feat/keystone-r4-integral) ----

[R4a DONE] Integral.lean compiles clean (lake env lean, no warnings); 8 headlines axiom-clean
([propext, Classical.choice, Quot.sound]): contDiff_pouCoeff, integrable_pouCoeff_mul,
resIntegral, resFunctional, det_complexToReal_smul_one, setIntegral_image_holomorphic,
setIntegral_overlap_relocate, setIntegral_overlap_relocate'.
THE FUNCTIONAL: pouCoeff 𝔇 j := indicator (chartMap j '' U_j) (rhoC 𝔇 j ∘ (chart j).symm) —
the indicator cutoff is what makes the chart-pushed weight an HONEST global planar function
(junk values of (chart j).symm outside the target are killed because tsupport ρ_j ⊆ U_j, so
the cutoff only removes zeros); globally ContDiff + HasCompactSupport, hence each summand
∫ ρ̃_j·t_j integrable from IsOneOneCoeff smoothness alone.
resIntegralFun t := ∑_j ∫_ℂ pouCoeff 𝔇 j z * t j z; resIntegral : oneOneCoeff 𝔇 →ₗ[ℂ] ℂ
(map_add via integral_add over the member integrability; map_smul via integral_const_mul);
resFunctional := resNormalization • resIntegral 𝔇 — R0 constant CITED, not re-derived.
RELOCATION: setIntegral_image_holomorphic (∫_{φ''s} u = ∫_s normSq φ′ · u∘φ) from
integral_image_eq_integral_abs_det_fderiv_smul + det_complexToReal_smul_one
(det_ℝ(c·) = normSq c via LinearMap.det_restrictScalars + Algebra.norm_complex_apply — the
exact scoping-inventory route); headline setIntegral_overlap_relocate: over overlapImage
(chart reads of U_j ⊓ U_k, open/injective/holomorphic via the R1 transition atoms), the
f-weighted integral of t_j equals the chart-k read — the CoV Jacobian is consumed by
OneOneLawAt evaluated pointwise (Eventually.self_of_nhds). R1's normSq design paid off on
the nose. Gotcha for posterity: chartMap j x vs (chartAt ...) x are defeq but not syntactic;
every left_inv/right_inv rewrite needs a TYPED have (hli : ... = x := left_inv ...), and
setIntegral_congr_fun goals need dsimp only to beta-reduce before rw.

[R4b DONE] OmegaWitness.lean compiles clean; 3 headlines axiom-clean:
localRep_transitionMap, isOneZeroCoeff_omegaCoeff, exists_omegaCoeff_ne_zero.
THE ω₀ WITNESS: omegaCoeff 𝔇 α j := coeffAt α (𝔇.center j) (the PROVEN
Montel.localRep/FormCoeff layer). The (1,0) law comes from localRep_transitionMap:
localRep α c_j = φ′_{jk}·localRep α c_k on U_j ⊓ U_k, proven from the frame identity
trivAt_symmL_one_eq_fderiv_C (SmoothPathCore: the chart-c unit tangent IS fderiv of the
chart transition) + fderiv chain rule through φ_{jk} (transition factors as
(chart_y∘chart_k⁻¹)∘φ near chart_j y) + ℂ-linearity of α.toFun y. Non-triviality at g ≥ 1:
exists_omegaCoeff_ne_zero via Module.nontrivial_of_finrank_pos (finrank = kirovGenus) +
exists_localRep_self_ne_zero + Montel.localRep_chart_transition/chartTransitionFactor_ne_zero.
K-POINT CONSTRAINT RECORDED WHERE IT BITES: the witness is UNCONDITIONAL (any cover, any α);
the refinement constraint is on the SCALAR w-data (HolomorphicOnOverlaps for Z¹(O_K) reps),
surfacing at R6 evaluation — recorded in the OmegaWitness module docstring next to the g=0
note, per the Gemini verdict's request.
G=0 (Gemini flag on #156): docs/planning/R4_G0_NOTE.md — lane R conditioned on
0 < kirovGenus X; NO fake witness, NO meromorphic-ω₀ generalization; genus 0 routes through
the snapshot SerreResidueDirectGenus0* leg (S9); the case split lives at the keystone
exists_serreDualityData instantiation (R7/R8), not inside lane R.

[R4 GATES] Both files wired into the KirovDolbeault root aggregator in the same commits
(F7 lesson); root lake build green (8758 jobs); all 11 R4 headlines #print-axioms standard-3.
R5 NEXT NEEDS: (i) the planar Stokes atom ∫_ℂ ∂̄g = 0 for g ∈ C^∞_c (Fubini via
volume_preserving_equiv_real_prod + intervalIntegral FTC on lines, or
integral2_divergence_prod_of_hasFDerivAt; GreenBox is the worked precedent); (ii) the
assembly: for a coboundary w = δh read through splitCoeff, t_j = ∂̄(β_j) with
β = ∑_k ρ̃_k·(h_k·ω₀)-coefficients; I(δh) = ∑_j ∫ ∂̄(ρ̃_j β_j) − ∑_j ∫ (∂̄ρ̃_j)·β_j; first
sum dies termwise by (i) (pouCoeff is ContDiff + compact support — use dbar_mul from R0 for
the Leibniz split); second sum dies by re-inserting the PoU (sum_rhoC_apply/sum_dbarRho via
the rhoC backbone) + setIntegral_overlap_relocate to re-route each (j,k) term + the cocycle.
Integrands are pouCoeff-weighted ⇒ all integrability free from R4's lemmas. Cite R0's
resNormalization; the functional to kill coboundaries on is resIntegral (normalization
commutes). Note: pouCoeff's ∂̄ is not yet packaged — R5 should add dbar_pouCoeff lemmas
(chart read of dbarRho) on top of contDiff_pouCoeff, or work with dbar (pouCoeff j) directly
via DbarDisk; support lemmas (support_pouCoeff ⊆ chartMap j '' tsupport ρ_j) are implicit in
pouCoeff_eq_zero_of_notMem_image_tsupport.

# ---- Session 4: R5 (feat/keystone-r5-stokes) ----

[R5a DONE] Stokes.lean compiles clean (lake env lean, no warnings); 9 headlines axiom-clean
([propext, Classical.choice, Quot.sound]). THE PLANAR ATOM: integral_dbar_eq_zero
(∫_ℂ ∂̄g dA = 0 for g ∈ C^∞_c) — NOT via GreenBox/Fubini-by-hand: Mathlib's
integral_mul_fderiv_eq_neg_fderiv_mul_of_integrable (Analysis.Calculus.LineDeriv.
IntegrationByParts, the prepackaged Fubini + line-FTC) applied against the constant 1 gives
∫ ∂_v g = 0 per direction (integral_fderiv_apply_eq_zero); ∂̄ = ½(∂_x + i∂_y) finishes.
No R5_BLOCKER needed — the IBP lemma's instance side conditions (IsAddHaarMeasure volume on ℂ)
resolved first try. THE PACKAGING GAP (flagged in the R4 handoff) closed:
dbar_pouCoeff_chartRead (on chartMap j '' U_j, ∂̄ρ̃_j = ∂̄ of the honest read ρ_j∘(chart j).symm)
+ dbar_pouCoeff_eq_zero_of_notMem_image_tsupport (frontier case, compact-tsupport clearance) —
same two-case pattern as contDiff_pouCoeff. PoU reinsertion: sum_dbar_rhoC_read
(∑_j ∂̄(ρ_j∘(chart k).symm) = 0 on the chart target — chart-read sum_dbarRho_eq_zero, derived
from the GLOBAL sum_rhoC_apply: the sum of reads is constantly 1 even through junk values).
Germ calculus dbar_congr_of_eventuallyEq / dbar_eq_zero_of_eventuallyEq_zero / dbar_finset_sum
+ globalization helpers contDiff_of_chartImage_clearance / contDiff_pouCoeff_mul /
contDiffAt_chartSymmRead / chartMap_image_U_subset_target for all downstream integrability.

[R5b DONE — THE R5 HEADLINE] CoboundaryVanish.lean compiles clean; 12 decls axiom-clean.
KILLS-COBOUNDARIES (verbatim):
  theorem resFunctional_eq_zero_of_coboundary (t : oneOneCoeff 𝔇)
      (ht : (t : 𝔇.toFiniteCover.ι → ℂ → ℂ) = glueCoeff 𝔇 w g)
      (hg : IsOneZeroCoeff 𝔇 g) (hsm : SmoothOnSets 𝔇 h) (hhol : HolomorphicOnSets 𝔇 h)
      (hδ : IsCoboundaryOn 𝔇 w h) : resFunctional 𝔇 t = 0
with IsCoboundaryOn 𝔇 w h := ∀ i j, ∀ x ∈ U_i ⊓ U_j, w i j x = h j x - h i x
(membership-guarded — the exact shape R7's germ extraction of a B¹(𝒪_K) coboundary delivers).
Proof: Forster collapse pouSplit_eq_of_coboundary (σ_i = h_i − β, β = pouAverage = ∑ρ_k h_k,
globally ContMDiff by the gdTerm pattern) ⇒ t_j = −∂̄(β̃_j g_j) on chart images; per-j Leibniz
split via R0's dbar_mul (integral_pouCoeff_glueCoeff_of_coboundary): ∂̄(ρ̃_j B_j)-term dies by
the R5a atom, survivor PoU-reinserted to (j,k) terms; per-(j,k) relocation
(integral_overlapTerm_relocate): vanish-off-overlap (ρ_k clearance / ∂̄ρ̃_j clearance) →
setIntegral_overlap_relocate (R4) on the (1,1) family ∂̄ρ_j ∧ (β·ω₀)
(isOneOneCoeff_dbarRead_mul: conj φ′ from dbar_comp on the relocated read, scalar β̃ invariant,
φ′ from the dz-slot; mul_conj reassembles normSq — same reconciliation as Glue) → re-extend by
the pouCoeff indicator; per-k kill (sum_integral_relocated_eq_zero) by sum_dbar_rhoC_read;
Finset.sum_comm glues. Coboundary data feeds glueCoeff_mem_oneOneCoeff for free
(smoothOnOverlaps/isOverlapCocycle/holomorphicOnOverlaps_of_coboundary), so R7 needs no side
conditions beyond (w, h, g) shape. DESCENT PACKAGING CHECKED: the port's quotient is
cechH1 = Z¹ ⧸ B¹.submoduleOf Z¹ with CousinResidueData.vanish_coboundary consumed by
Submodule.liftQ (GlobalResidueConstruct.lean:141-182, res_mk simp at :171);
resFunctional_eq_zero_of_coboundary is that field's chart-coefficient feeder — deliberately NOT
instantiated here (R7 scope: germ→coefficient extraction Z¹(𝒪_K) → overlap families, then liftQ).

[R5 GATES] Both files wired into the root aggregator in their own commits (f11f1a1 R5a,
2fe3103 R5b); root lake build green (8760 jobs); all 21 R5 decls #print-axioms standard-3
([propext, Classical.choice, Quot.sound]); no sorries; not pushed (orchestrator gates).
R6 NEXT NEEDS (BOTH pieces): (i) THE SIMPLE-POLE ML-TIE (S3 scoping §4.R6, the hardest rung):
for the explicit single-simple-pole ML cocycle e_{ij} = m_i − m_j with Res_a(ω₀·m) = r,
resFunctional evaluates to r — route: η = ∑ρ_k(ω₀ m_k)-coefficients, ContDiffBump cutoff ψ_a
in one chart, smooth part dies by R5a atom + R4 relocation, singular part is EXACTLY
DbarDisk.cauchyPompeiu_area at the pole (ψ_a(a)=1, no annulus limits); identify r via
resAt_const_mul_sub_inv; CITE resNormalization = −π⁻¹ (R0) — the R5b coboundary machinery
(IsCoboundaryOn is exactly the off-pole shape: away from a, e = δm honestly) plus the K-point
cover-refinement constraint recorded in Glue/OmegaWitness docstrings. (ii) THE UNWINDREGULARITY
DISCHARGE (SerreUnwind.lean:275, the §17.7 pole-bound regularity Prop, hypothesis-parametric)
— S5's SurjectivityInputs assembly is complete modulo it; under the Forster architecture its
discharge becomes an R6-style residue-evaluation estimate, so schedule it WITH the ML-tie, not
after R7. R7 then: germ→coefficient extraction + CousinResidueData instantiation (liftQ descent
is already proven in the port).

=== 2026-06-10 R6D2 lane (branch feat/keystone-r6d2, worktree agent) ===
START. Four deliverables: D2a order-m vanish tie (generalized mero-coboundary engine,
removable-product Stokes kill — NO higher Cauchy-Pompeiu needed: slot vanishing makes the
smeared-pole product extend analytically, so the surviving Stokes term dies by the planar
Stokes atom after a finite point-repair); D2b general-K vanish_coboundary (principal parts
avoided: restrict 0-cochain germs to U_i \ S, holoFn-extract, product-germ trick at K-points:
(f·slot-pullback) ∈ OmegaDGerm 0 → holoFn gives the analytic extension q); D2c
UnwindRegularity; D2d CupMLWitness. Plan: new FineResidue/MeroVanish.lean (engine, mirrors
MLTie/CoboundaryVanish), then Descent-side wiring.

2026-06-10 D2a LANDED: MeroVanish.lean compiles clean (4 iterations). Engine
resFunctional_eq_zero_of_mero_coboundary + order-m tie
resFunctional_poleCocycle_eq_zero_of_slot_vanishes. Key design win: NO higher-order
Cauchy-Pompeiu — pointRepair (finite limit-repair) + planar Stokes atom kills the
surviving term since the slot zero makes the smeared product extend analytically.
Next: D2b DescentVanish.lean — general-K vanish_coboundary via holoFn extraction off
the K-points + product-germ trick ((f·slot-pullback) ∈ OmegaD 0, holoFn = the analytic
extension q); also the R6Outputs bridge (MLTie is on this branch).

2026-06-10 D2b LANDED: DescentVanish.lean compiles clean (3 iterations). Headlines:
- resCocycle_vanish_coboundary: vanish_coboundary at GENERAL K (R7 gap 1 closed) via
  product-germ trick — no Laurent decomposition: restrict germs off K-points, holoFn
  extract; at each K-point (f·slot-pullback) ∈ OmegaD 0 (orders −K a + K a ≥ 0; slot
  order via dslope factorization + meromorphicOrderAt_mul/pow_id_sub_const), holoFn of
  the product germ IS the engine's analytic extension q.
- r6Outputs_holds: Descent.R6Outputs now a THEOREM (MLTie bridge, Subtype.ext).
- resH1_of_slotMatches: unconditional liftQ descent on cechH1 K.
- CupMLWitnessR + nondegenerate_of_witnessR + cousinResidueData_of_witnessR: CORRECTED
  §17.6 witness interface — original CupMLWitness demands g(α)=1 exactly (level set can
  be empty: likely unsatisfiable for fixed g); fixed to r·g(α)=1 with free residue r;
  membership conjunct now proven. CousinResidueData reduced to CupMLWitnessR alone.
D2c (UnwindRegularity) + D2d construction half: genuine walls, documented in
R6D2_BLOCKER.md (forced non-isolated point + HIGHER-ORDER Cauchy-Pompeiu with NONZERO
conclusion needed for 17.7; isolated-point selection/cover refinement for the witness).

=== 2026-06-10 final-bits lane (feat/keystone-final-bits) ===
[start] Items: (1) SlotMatchesK witness slotMatchesK_omegaCoeff; (2) cover construction
  (exists_separatingChartDiskCover + reserved disk, generic isLeray, generalized
  locallyRealizable); (3) CupMLWitnessR over the reserved disk; (4) hga if time.
  Caches APFS-cloned, 264G free. Beginning item 1 (SlotMatch.lean).
[item1 DONE] SlotMatch.lean: slotMatchesK_omegaCoeff PROVEN (standard-3) via
  formOrderW_chart_invariant + formCoeff_holToSection + coeffAt_analyticAt +
  AnalyticAt.meromorphicOrderAt_eq + analyticOrderAt_eq_natCast factorization.
  Wrappers resH1_omegaCoeff / cousinResidueData_omegaCoeff: analytic side of the
  R-lane descent fully discharged for g = omegaCoeff; remaining hyps = SeparatesPoles
  (cover geometry, item 2) + CupMLWitnessR (item 3). Aggregator wired. Starting item 2.
[item2a DONE] ChartDiskCoverGeneric.lean: generic ChartDiskCover.isLeray (every chart-disk
  cover set is a chart-ball preimage, simply connected) + generic
  ChartDiskCover.locallyRealizable (factorized-rational product witness, only cover-specific
  input was subset_chart_source). Bill items 2+3 of COVER_WIRING done. Next: the
  separating-cover construction with the reserved disk (bill item 1).
[item2b DONE] SeparatingCover.lean: exists_separatingChartDiskCover PROVEN (standard-3).
  Construction: reserved point p ∉ S, reserved disk in U(idx p) ∩ Sᶜ, compact half-radius
  core, good disks in U(idx x) ∩ coreᶜ ∩ (S.erase x)ᶜ, finite subcover of (resDisk)ᶜ,
  Option-indexed assembly. Outputs: refinement of any 𝔘 + S-separation (→ SeparatesPoles
  for supp K ⊆ S) + nonempty open reserved zone W (→ MLIsolated ∀ w ∈ W) + W ∩ S = ∅.
  Item 2 complete (with 2a generics). Next: item 3, CupMLWitnessR over the reserved disk.
[item3 DONE — THE LANE HEADLINE] CupWitness.lean: CupMLWitnessR INHABITED
  (cupMLWitnessR_of_isolated, standard-3). The cup_surjective_of_ne_zero dissolution
  worked exactly as scoped: NO transport computation — build the explicit germ-level
  one-point ML cocycle mlCochain (toGerm of mlCocycle; Z¹(𝒪_K) membership = pointwise
  ring identity through toGerm + MLIsolated keeps the pole off overlaps + K ≥ 0),
  extraction agreement by eq_at_of_toGerm_eq (germ classes pin continuous reps),
  r := g j₀(α)⁻¹ normalization by inv_mul_cancel₀, and cup surjectivity supplies ξ
  hitting [mlCochain] for every (D, v). ω₀-side inputs: formDivisor_nonneg (div ω₀ ≥ 0)
  + omegaCoeff_ne_zero_at (slot ≠ 0 where K = 0 — every reserved-zone point).
  CAPSTONE exists_separating_cousinResidueData: for any 𝔘 and α with divisor K,
  a refining separating cover (Leray + locallyRealizable) with hsep, inhabited
  CupMLWitnessR, and Nonempty (CousinResidueData 𝔇.toFiniteCover K) — ZERO remaining
  hypotheses. All 6 decls standard-3; root aggregator wired; module lake build green.
  Next: item 4 (hga via D=0 Čech↔Dolbeault comparison) if time.
[item4 PARTIAL — honest scope] ArithmeticGenusTransfer.lean: the hga ATOM ITSELF is NOT
  provable from the comparison alone (confirmed R6D2_BLOCKER §3 / G0_BLOCKER route 2: needs
  H^{0,1}=0 from no-holomorphic-forms, Hodge-flavoured — research-grade, NOT faked). What IS
  proven (standard-3): the cover dependence of hga is GONE — the IsLeray-free comparison
  (GoodCover.cechH1_dolbeault_comparison', finrank ℝ DolbeaultH01 = 2·h1Dim 0, cover-free
  LHS) gives h1Dim_zero_chartDiskCover_invariant (h¹(𝒪) equal on ALL chart-disk covers),
  hga_transfer (atom at any one cover feeds every other, incl. the separating cover), and
  h1Dim_zero_eq_canonical (normal form at the canonical chartDiskCover). The genus-0 wall
  need only be climbed once, at any convenient cover. Root lake build green (8771 jobs).
  LANE COMPLETE: items 1-3 DONE, item 4 done to its honest boundary.

=== 2026-06-10 UNWIND lane (branch feat/keystone-unwind, worktree agent) ===
START. Target: UnwindRegularity G D (SerreUnwind.lean:275) for the concrete fine-sheaf G.
Route fixed after full read of MLTie/MeroVanish/Descent/DescentVanish/SkyscraperConeRealization/
LocalRealization: (1) detector reduction (kernel class with nonzero pairing ⟹ UnwindRegularity,
pure logic); (2) bad point b forced with E b ≤ n+K b ≤ D b−1, n = ord_b f; (3) test cocycle
δ⁰(Pi.single j₀ γ) with γ the LocallyRealizable witness germ in 𝒪_{Ě+b}(U j₀), Ě = E+(m−E b)·b —
KEY FIND: LocallyRealizable (proven, SkyscraperProductWitness) solves the E-negative-zeros
membership wall for free; (4) evaluation = MeroVanish engine generalized to ONE marked
simple-pole point (SlotProductSimplePoleAt, conclusion −r) — no CP ladder needed (Leibniz
absorption + R0 atom + pointRepair). Residual wall: evaluation at NON-isolated forced points
(multi-chart smeared pole; cochain side already covered by skyscraper coneB0). Plan in
docs/planning/UNWIND_ROUTE.md. Disk note: machine at 100% (18 sibling worktrees); using
cp -Rc clones + lake env lean only.

2026-06-11 UNWIND resume: Commit A landed (01b3203) — marked-divisor bookkeeping:
separatesPoles_add_single, mem_linearSystem_marked (exact −n bound at b), cup_mem_sections0
(cup 0-cochain ∈ sections0(K+b)), cupRep + toGerm_cupRep + ordU_cupRep (exact order −K b−1).
Next (Commit B): holoFn_eventuallyEq_near_marked (read-back of the analytic rep on a punctured
nbhd of the marked point via finite-order normal form + holoFn_eq_of_tendsto), then
exists_slotProductSimplePoleAt (order −1 exact ⟹ residue w'(α) ≠ 0 via dslope split), then
resCocycle_cup_testCocycle_ne_zero through resFunctional_eq_neg_residue_of_mero_coboundary
(mixed-level trick: z ∈ Z¹(K) is a B¹(K+b)-coboundary; cocycleFn proof-irrelevant ⟹ rfl).

2026-06-11 UNWIND Commits B+C LANDED (3e7ca31, 62948e0, + headline):
- holoFn_eventuallyEq_near_marked: the read-back principle — holoFn of a class on W agrees
  with Gext of ANY honest rep on a punctured nbhd of a marked boundary point b ∉ W, given
  finite meromorphic order (normal form ⟹ junk-free continuous formula ⟹ limUnder recovers).
- exists_slotProductSimplePoleAt: exact orders (−K b−1) + K b = −1; order −1 ⟹ residue
  w'(α) ≠ 0; q = dslope w' α; pure-ring final identity (div_eq_mul_inv, no field_simp).
- resCocycle_cup_testCocycle_ne_zero: the §17.7 evaluation. Mixed-level trick WORKED AS
  PLANNED: cup cocycle ∈ Z¹(K) viewed in Z¹(K+b) (OmegaDGerm_mono), and
  cocycleFn 𝔇 hsep z = cocycleFn 𝔇 hsep' z' is rfl (proof irrelevance through holoFn/choose).
  Engine = resFunctional_eq_neg_residue_of_mero_coboundary at S = posSupp(K+b).
- unwindRegularity_concrete_of_isolated: THE CONDITIONAL HEADLINE — UnwindRegularity G D is a
  THEOREM for the concrete fine-sheaf G, conditional on BadPointsIsolated 𝔇 K D (+ the R-lane
  interface hsep/hg/SlotExactK/CupMLWitnessR/ExactOrderWitness(proven)/K ≥ 0).
- Gates: #print axioms standard-3 on all six headliners; 0 sorries; full lake build green
  (8765 jobs); SerreUnwindDetect wired into KirovDolbeault.lean root imports.
- Residual wall documented in docs/planning/UNWIND_BLOCKER.md: unconditional ∀ D needs the
  multi-chart (non-isolated) evaluation — per-chart smeared-pole atom with non-constant weight
  + cross-chart residue transport + ∑ρ≡1 telescoping; cochain side already covered (coneB0).
NOTE: stale-olean gotcha — lake env lean used pre-branch MeroVanish.olean until
`lake build ...FineResidue.DescentVanish`; "unknown identifier SlotProductSimplePoleAt" was
NOT a namespace problem.

=== 2026-06-11 TAIL lane resume (branch feat/tail-duality-tower) ===
RUNG 4 LANDED FIRST (shapes lined up directly — short, as hoped): TailUnwind.lean.
- CechTailComparison: the RESTATED §17.9 chain input — pure Čech↔tail functional dictionary
  (factorization through H¹(𝒪_E)→H¹(𝒪_D) ⟹ slot tail pairings vanish on the gap window),
  NO regularity content (that is rung 2's theorem).
- GlobalResidue.unwindRegularity_of_cechTailComparison: comparison + SlotExactK + K≥0 ⟹
  UnwindRegularity G D, via tailRegularitySlot_lSysInclMono. No BadPointsIsolated anywhere.
- pairing_surjective_of_cechTailComparison: end-to-end §17.9 surjectivity with the re-pointed
  input (UnwindRegularity no longer an input of the chain).
- Gates: lake build green (8744 jobs); both headliners standard-3; aggregator wired.
Next: RUNG 3 — cup-kill (exists effective A with h¹(𝒪_A)=0, Kirov CechH1CupKill idea, our
implementation over cup/h1InclMono/RR), then the conditional two-RR subtraction targets.

2026-06-11 RUNG 3 LANDED (CechH1CupKill.lean + TailGenusTarget.lean):
- CUP-KILL PROVEN (no hypotheses): exists_effective_h1InclMono_eq_zero (one-class kill —
  RR pigeonhole on the cup-at-ξ map L(nP)→H¹(𝒪_nP) at n=h¹(𝒪): l(nP)=h¹(nP)+1 forces a
  kernel class ψ≠0; then incl_{0→A} ξ = ψ⁻¹⌣(ψ⌣ξ) = 0 at A = nP + div ψ via
  globalGerm_mul_inv — the cupH1_surjective factorization run in the killing direction)
  and exists_effective_h1Dim_eq_zero_forall_ge (effective A₀ with h¹(𝒪_A)=0 ∀ A ≥ A₀:
  basis kill + summed divisors + h1InclMono_surjective onto-kills-basis).
- TWO-RR SUBTRACTION EXECUTED, conditional on ONE named input TailRiemannRoch X
  (large-degree tail RR, the item-3 tower output — see TAIL_BLOCKER.md):
  h1Dim_zero_eq_kirovGenus_of_tailRR (uniform h¹(𝒪)=g at ANY locally realizable cover),
  h1Dim_zero_eq_zero_of_kirovGenus_zero (the hga shape, target (a)),
  h1Dim_zero_chartDiskCover_eq_kirovGenus (canonical-cover normal form — the EXACT
  port-side fact the Layer-3 flip of h1coh_zero_finrank consumes at the CechH1Bridge
  cover pin), h1Dim_zero_anyChartDiskCover_eq_kirovGenus (via ArithmeticGenusTransfer).
- KEYSTONE COLLAPSE: exists_serreDualityData_of_genus_zero_of_tailRR (g=0 leg, hga gone)
  and exists_serreDualityData_genus_split_of_tailRR (keystone = hpos + TailRiemannRoch;
  hga atom discharged; hpos's UnwindRegularity ∀D is what rung 4 re-points).
- Gates: all 8 headliners standard-3 (#print axioms); TailGenusTarget lake build green
  (8733 jobs); both files wired into KirovDolbeault.lean aggregator; TAIL_BLOCKER.md
  records the two residual inputs (TailRiemannRoch; CechTailComparison) honestly.
