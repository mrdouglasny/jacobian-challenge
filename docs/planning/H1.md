# `H1` — discharge recipe

**Location:** `Jacobians/RiemannSurface/LineBundle.lean:104`
**Route:** mathlib-now &nbsp;&nbsp; **Effort:** 2 &nbsp;&nbsp; **Est:** ~1 focused day, ~20 LOC (definition via Mathlib's derived functor sheaf cohomology)
**Blocked by:** `LineBundle` (`Jacobians/RiemannSurface/LineBundle.lean:77`), `Divisor` (`:51`)

**Statement (verbatim):**
```lean
/-- **Opaque axiom type.** The first sheaf cohomology `H¹(X, L)` of a
line bundle `L`. Classically: Čech cohomology over a Leray cover. -/
axiom H1 {X : Type*} [TopologicalSpace X] [T2Space X]
    [CompactSpace X] [ConnectedSpace X] [ChartedSpace ℂ X]
    [IsManifold 𝓘(ℂ) ω X] {D : Divisor X} (L : LineBundle D) : Type
```

**Why it's an axiom right now:** `H¹(X, L)` is the first sheaf cohomology of the analytic line bundle `L = 𝒪(D)`. The previous draft of this plan incorrectly assumed that we needed to construct the Čech complex by hand and prove the Cartan–Serre finiteness theorem before we could even define the type. By decoupling the definition from the finiteness theorem, we can define `H1` purely via Mathlib's existing derived-functor sheaf cohomology on the site of open sets of `X`.

**Proof recipe**

1. **Ensure `LineBundle` yields a sheaf.** Following the `LineBundle` discharge, `L` must be able to produce its sheaf of sections, `L.sheaf`, typed as a `Sheaf (ModuleCat ℂ)` over the topological space `X` (viewed as a Grothendieck site). 
2. **Import Mathlib cohomology.** Use `Mathlib.CategoryTheory.Sites.SheafCohomology.Basic`, which defines `Sheaf.H F 1` globally for any abelian sheaf without requiring a choice of cover.
3. **Define `H1`.** In `Jacobians/RiemannSurface/LineBundle.lean:104`, replace the `axiom H1` with:
   ```lean
   def H1 {X : Type*} [...] {D : Divisor X} (L : LineBundle D) : Type :=
     (L.sheaf).H 1
   ```
4. **Discharge instances.** The module and group structure axioms (`H1.instAddCommGroup` at `:108`, `H1.instModule` at `:114`) immediately follow from the fact that `Sheaf.H` for a sheaf of `ModuleCat ℂ` canonically inherits the `Module ℂ` structure. These become `instance` declarations with `inferInstance` bodies (or simple forgetful functor projections).

**Files touched**
- `Jacobians/RiemannSurface/LineBundle.lean` — replace `axiom H1` (line 104) with `def H1 L := (L.sheaf).H 1`; convert the companion `H1.instAddCommGroup` (`:108`) and `H1.instModule` (`:114`) axioms to `instance`s using Mathlib's existing API.

**Acceptance**
- `lake build Jacobians.RiemannSurface.LineBundle` succeeds with `H1` as a `def`, not an `axiom`.
- `#print axioms AX_RiemannRoch` (`Jacobians/Axioms/RiemannRoch.lean:59`) no longer lists `H1`; `#print axioms AX_SerreDuality` (`Jacobians/Axioms/SerreDuality.lean:54`) no longer lists `H1`.
- `python3 gate.py --repo jacobian-challenge --build Jacobians` returns PASS; axiom count drops by 1 (and by ≥ 2 more once the companion `H1.instAddCommGroup` / `H1.instModule` recipes collapse alongside).

**Risk / escalation triggers**
- If `L.sheaf` does not neatly form a `Sheaf (ModuleCat ℂ)` that meshes with Mathlib's `Sheaf.H` site machinery without severe universe bumps or typeclass diamonds, escalate to coordinate the category definitions.
- If Lean complains about missing projective/injective resolutions merely to *instantiate* the definition of `Sheaf.H 1`, escalate. (Mathlib typically defines derived functors assuming enough injectives exist abstractly, but universe parameters sometimes cause inference failures).

**Gemini critique addressed:**
- **Route & Effort:** Changed route to `mathlib-now` and reduced effort to 2, utilizing Mathlib's `Sheaf.H F 1` out of the box instead of reinventing the wheel.
- **Valid Type Theory:** Defined `H1` via derived functors, averting the fatal dependent-type error where the Lean type would otherwise explicitly depend on the choice of a specific Leray cover.
- **Topological Corrections:** Removed the mathematically invalid assumption that a 2-manifold cover could have empty triple intersections ($C^2 = 0$).
- **Decoupled Finiteness:** Extracted the massive Cartan–Serre finiteness proof (which does require Čech complexes and Montel's theorem) from the definition of the type itself, relegating it to a separate downstream sub-plan.

## Sub-plans needed
- **`CartanSerreFiniteness`:** A downstream theorem to prove `FiniteDimensional ℂ (H1 L)`. This will require building the bounded Čech complex, keeping $C^2$ valid for a 2-manifold, proving Leray's theorem as an isomorphism to `Sheaf.H 1`, and applying Montel's theorem.

---
**Vetting trail.** Critique: `_vetting/H1.md`. Verdict: reject. Revised: 2026-06-03.