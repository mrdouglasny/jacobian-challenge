# SVKGEN lane — issue #171 B1: puncture-lasso generation
# (HANDOVER_PARALLEL_ACCOUNT.md UPDATE 3, Package 7; route: B1_GENERATION_ROUTE.md)

Append-only progress log. Branch: `feat/svk-generation`, based on main @ 399a243.

## 2026-06-12 session 1

- Route doc landed (B1_GENERATION_ROUTE.md, posted to issue #171): G1-G5,
  generation-half-only extraction, G4 normal-closure decision pinned.
- G1 DONE: `Jacobians/Topology/CoverGeneration.lean` —
  - `fromPath_mem_of_loops_mem`: over an open cover with path-connected
    pairwise intersections all containing x₀, a subgroup containing every
    single-cover-set loop class contains every loop class (generation half
    of van Kampen, membership form). Extraction of the FiniteGoodCover
    engine with the anchor layer removed; compiled clean on first attempt.
  - `fromPath_mem_of_two_open`: the classical two-open corollary (ι = Bool).
  - Kernel: both standard-3. Umbrella wired (Jacobians/Topology.lean).
- Next (G2): cell computation — spoke-conjugated loop in a once-punctured
  cell is a lasso power, via pi1PuncturedPlaneIntAt + pi1MulEquivOfHomeomorph.

## 2026-06-12 session 1 (cont.)

- PR #212 (G1) opened; CI green (22m55s).
- G2 DONE: `Jacobians/Topology/CellLassoPower.lean` —
  `fromPath_eq_cellLasso_zpow`: a loop staying inside a cell homeomorphic to
  a once-punctured plane is, in pi1 of the ambient space, an integer power of
  the cell lasso (the inclusion image of the phi-pullback of circleAround).
  Assembly: pi1PuncturedPlaneIntAt + generator pin + pi1MulEquivOfHomeomorph
  + mapOfEq functoriality; helpers pathCorestrict / inclusionCM /
  mapOfEq_inclusionCM_pathCorestrict. Kernel: standard-3. Umbrella wired.
- Pushed to the same branch (PR #212 becomes G1+G2; un-reviewed, so one
  SVKGEN PR keeps review surface in one place).
- Next (G3/G4): half-plane homeo toolkit + separating-line induction in the
  normal-closure form.

- G3 toolkit (half 1) DONE: `Jacobians/Topology/HalfPlaneHomeo.lean` —
  `halfPlaneHomeo c : {z : ℂ // z.re < c} ≃ₜ ℂ`, explicit componentwise
  (−log(c−Re), Im) with inverse (c−exp(−Re), Im). Kernel standard-3.
  Remaining toolkit: finite-puncture restriction of a homeo; then the G3
  separating-line induction + G4 normal-closure bookkeeping.
- Note from main: TD-lane discharged TR-DISC (#214) and the Abel P-block
  completed (#213) — the cycle-basis program is converging from the other
  side; B1 generation remains on the critical path for the slit-sheet route.

- REVIEW FLAG (maintainer pre-verification, PR #212): the sed-based umbrella
  wiring silently no-opped — Jacobians/Topology.lean never imported the new
  modules, so per-file umbrella checks were green VACUOUSLY and root CI never
  compiled them. Fixed with the Edit tool + grep verification; root build now
  8,962 jobs (+3 over main). Gotcha: always grep-verify sed edits or use the
  Edit tool; vacuous-green umbrella checks are invisible without a job-count
  comparison.
- Maintainer pre-verification on #210/#211: CLEAN, awaiting external review.

- n=1 anchor DONE: `closure_circleAround_eq_top` (CellLassoPower.lean) —
  pi1 of the once-punctured plane is generated (plain closure) by the
  explicit circle class. Standard-3. This is the base case of the packaged
  induction (route doc G3/G4 notes, option (b)); the induction step is the
  next session-scale item.

## 2026-06-12 session 1 (cont.) — post-#212-merge, induction lane opened

- #212 MERGED (review blocker was a stale-head read; maintainer resolved).
  Handover UPDATE 4: critical-only, P8 = this chain, then P9/P10 witnesses.
- New branch feat/svk-induction (off the #212 merge). Statement layer landed:
  `Jacobians/Topology/PuncturedPlaneGeneration.lean` —
  - `PuncturedCellSystem T x₀`: per-puncture once-punctured cell ∋ x₀ with
    the DUAL pin (cell-lasso + winding ±1 self / 0 off) — the conjunction
    that evades the perfect-group trap while tying cells to punctures.
  - `normalClosure_lassos_eq_top_of_empty` + the simply-connected
    `T = ∅` engine; both standard-3. Umbrella wired (Edit tool, verified).
- Next: |T| = 1 case (closure_circleAround_eq_top + winding pins force
  cellLasso = gen^{±1}), then the binary-split induction step.

- |T| = 1 base case DONE: `normalClosure_lassos_eq_top_of_singleton` —
  the whole once-punctured plane is itself an admissible cell (Set.univ +
  singletonComplHomeo), so G2 makes pi1 cyclic on the universe lasso; the
  winding pin + integer-unit algebra force any admissible system's lasso
  exponent to ±1, hence plain closure = top, NO winding computation needed.
  Standard-3. Remaining: the binary-split induction step (the last rung).

## 2026-06-12 TOPO lane (takeover, P8 queue) — G3/G4 induction session

- ARCHITECTURE DECISION (continuing route-doc option (b) + its own G4
  "strengthen the induction over ALL basepoints/spokes" note): run the
  binary-split induction in ABSTRACT-SUBGROUP MEMBERSHIP FORM (the G1
  house pattern) with spoke-conjugated generators. All transports
  (side-homeo, basepoint, rotation) become `Subgroup.comap` along pi1
  homs — no normalClosure-mapping lemmas, and the meridian-conjugacy
  "mountain" (4th-pass filling kernel) is NOT needed for the concrete
  headline: generation by spoked small circles. The 2D telescope remains
  only for the arbitrary-PuncturedCellSystem statement (non-blocking,
  Mathlib-grade generality).
- SpokeAlgebra.lean LANDED: spokedClass + complete algebra (transport
  eval, composition law for fundamentalGroupMulEquivOfPath, refl law,
  spoke-congruence, quotient assoc/cancel, composite-spoke peeling,
  naturality under maps). Kernel standard-3 on all 9 decls. Umbrella
  wired (Edit tool, grep-verified).
